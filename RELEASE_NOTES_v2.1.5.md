# AI Chat Exporter v2.1.5 — DeepSeek、元宝与豆包消息提取修正

## 本次更新

- DeepSeek：排除宿主旁侧问题导航列表，避免将导航条目重复计入插件问题目录；目录与导出的三份选择器配置同步。
- 元宝：思考过程保留在独立字段，不再重复进入导出正文。处理发生在克隆节点上，不删除宿主页面内容。
- 豆包：识别到的纯图片用户消息以 `[图片]` 占位保留，并记录图片数量；不包含图片文件导出能力。

## 验证与边界

- 9 个自动化测试文件通过，覆盖新增选择器、思考分离以及已有索引、路由、跳转和清理契约。
- 2026-09-07 Chrome 真实会话核查覆盖当前挂载窗口：DeepSeek 正文消息边界、元宝思考/正文层级、豆包纯图片用户节点。
- 修正版扩展的冷首次打开、A → B → A 切换、流式、长会话导航、真实下载与侧栏关闭清理尚未完成现场验收；不能据此声明完整会话覆盖或端到端通过。

## 安装与更新

下载附件 ZIP，解压后通过 Chrome / Edge 扩展管理页加载已解压的扩展。更新后刷新已打开的会话标签页。

本次为 GitHub Release；不代表 Chrome Web Store 或 Edge Add-ons 已更新。
